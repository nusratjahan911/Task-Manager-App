import 'package:flutter/material.dart';
class circuler_progress_indicator extends StatelessWidget {
  const circuler_progress_indicator({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Center(
      child: CircularProgressIndicator(),
    );
  }
}